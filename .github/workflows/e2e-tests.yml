name: E2E Tests with Coverage

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  e2e-tests:
    name: E2E Tests with Coverage
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
          POSTGRES_DB: ${{ secrets.POSTGRES_DB }}
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'
      
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: |
            frontend/package-lock.json
            e2e-tests/package-lock.json
      
      - name: Install backend dependencies and build
        run: |
          cd backend
          mvn clean install -DskipTests
      
      - name: Start backend in background
        run: |
          cd backend
          nohup mvn spring-boot:run > ../backend.log 2>&1 &
          echo $! > ../backend.pid
        env:
          SPRING_DATASOURCE_URL: jdbc:postgresql://localhost:5432/${{ secrets.POSTGRES_DB }}
          SPRING_DATASOURCE_USERNAME: ${{ secrets.POSTGRES_USER }}
          SPRING_DATASOURCE_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
      
      - name: Wait for backend to be ready
        run: |
          timeout 120 bash -c 'until curl -f http://localhost:8080/api/health; do sleep 5; done' || echo "Backend health check failed, continuing anyway"
      
      - name: Install frontend dependencies
        run: |
          cd frontend
          npm install
      
      - name: Start frontend in background with coverage
        run: |
          cd frontend
          nohup npm run dev > ../frontend.log 2>&1 &
          echo $! > ../frontend.pid
        env:
          VITE_COVERAGE: 'true'
      
      - name: Wait for frontend to be ready
        run: |
          timeout 60 bash -c 'until curl -f http://localhost:8080; do sleep 5; done' || echo "Frontend health check failed, continuing anyway"
      
      - name: Install E2E test dependencies
        run: |
          cd e2e-tests
          npm install
      
      - name: Install Playwright browsers
        run: |
          cd e2e-tests
          npx playwright install --with-deps chromium
      
      - name: Run E2E tests
        run: |
          cd e2e-tests
          npx playwright test
        env:
          BASE_URL: http://localhost:8080
      
      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: e2e-tests/playwright-report/
          retention-days: 30
      
      - name: Upload Playwright traces
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-traces
          path: e2e-tests/test-results/
          retention-days: 30
      
      - name: Upload backend logs
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: backend-logs
          path: backend.log
          retention-days: 7
      
      - name: Upload frontend logs
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: frontend-logs
          path: frontend.log
          retention-days: 7

      - name: Upload frontend coverage report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: frontend-coverage-report
          path: frontend/coverage/
          retention-days: 30
      
      - name: Stop backend and frontend
        if: always()
        run: |
          if [ -f backend.pid ]; then
            kill $(cat backend.pid) || true
            rm backend.pid
          fi
          if [ -f frontend.pid ]; then
            kill $(cat frontend.pid) || true
            rm frontend.pid
          fi
